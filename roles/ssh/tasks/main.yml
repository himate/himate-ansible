- name: Add sshd_config
  template:
    src=templates/sshd_config.j2
    dest=/etc/ssh/sshd_config
    owner=root
    group=root
    mode=644
  notify:
    - restart sshd
  when: "{{ configure_sshd | default(True) }}"
  vars:
    all_groups: "{{ access_management.groups }}"
